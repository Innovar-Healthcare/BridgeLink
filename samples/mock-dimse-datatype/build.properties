# Extension identity
plugin.version=1.0.0
plugin.name=mock-dimse-datatype
plugin.path=mock-dimse-datatype
plugin.package.name=${plugin.name}-${plugin.version}

# Build output
build.dir=build
classes.dir=${build.dir}/classes
dist.dir=${build.dir}/dist

# BridgeLink dependency paths (relative to this sample)
server.lib=../../server/lib
server.classes.dir=../../server/classes

# Local dev install location
server.setup.extensions=../../server/setup/extensions
